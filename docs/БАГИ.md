# БАГИ — единый реестр

**Сведено 2026-08-31** из `docs/АУДИТ.md` (§2, §4, §5, §9, §10, §11, §12),
`docs/АРХИТЕКТУРА-2026-08-30.md` §10–§11 и `README.md` §12.

> Этот файл — индекс. Детали и обоснование — в исходных документах.
> Состояние пула — `docs/АУДИТ.md` §7. Текущее состояние кода проверять
> `python -m pytest tests/ -q` и `python scratch/_doc_audit.py`, а не по этому файлу.

## Счётчик

| | |
|---|---:|
| Всего записей | **66** |
| ✅ Исправлено | **60** |
| ❌ Не исправлено | **5** |
| ⛔ Снято (дефекта не было) | **1** |

Легенда серий: **D** — дефекты полного аудита · **R** — раунд 2 · **BUG** — раунд 1 ·
**G** — раунд 6 · **A** — скорость (`docs/ИССЛЕДОВАНИЕ-СКОРОСТЬ.md`).

Нумерация `BUG-7…BUG-11` отсутствует — номера перескакивают ещё в исходном АУДИТе.

---

## ❌ НЕ ИСПРАВЛЕНО (5)

Все пять об одном: **движок готов, носители пусты**.

| ID | P | Что | Где смотреть |
|---|---|---|---|
| **A4** | P1 | Второй setupwoo-донор в EU/US. Весь `$0`-вектор на одном австралийском сайте, EMA-латентность **6 111 мс**. Единственная точка отказа. | `АУДИТ.md §5`, `§7.1` |
| **D-4** | P1 | `piconfirm` без цели: `pi_target.txt` нет, `pi_gates.json` пуст. Зарегистрирован, гарантированно `ERROR`. | `АРХИТЕКТУРА §10` |
| **D-5** | P1 | `braintreenvbv` без целей: `data/braintree_targets.txt` — 0 байт. Скан 572 доменов дал 13 ложных срабатываний (Google/Facebook-аналитика в base64). | `АРХИТЕКТУРА §10` |
| **D-6** | P1 | Прокси-пул пуст. `ProxyPool` целиком готов (sticky на донора, веса 1/latency, health-файл), но `data/proxies.txt` пуст, а сырой `proxies_https_60k.txt` не читает ни один модуль. Весь трафик с домашнего IP. | `АРХИТЕКТУРА §10` |
| **D-10** | P3 | Мёртвые носители: `data/results/*.jsonl`, `data/pi_gates.json`, `data/hit_targets.txt` пишутся и не читаются. Примыкает битый `research/ses_fcab092e0ffeNsD51wEuJOPoAJ.json` (2 МБ, валиден до 1 966 147 символа, дальше 81 188 символов хвоста). | `АРХИТЕКТУРА §10` |

**Порядок отдачи:** D-6 → A4 → D-4/D-5 → D-10.
D-6 единственный пункт, который улучшает всё разом: скорость, живучесть доноров, опек.

---

## ⛔ СНЯТО (1)

| ID | P | Что | Почему снято |
|---|---|---|---|
| **D-13** | P2 | `requirements.txt` требует `kurigram`, код импортирует `pyrogram` — якобы чистая переустановка ломает бота | **Дефекта не было.** kurigram — форк pyrogram, отдающий тот же импортируемый неймспейс. Проверено на 2.2.25: `import pyrogram` → 2.2.25, `Client`/`filters`/`ParseMode`/`Message` живы. Оригинальный pyrogram не собирается под 3.13+. **Менять kurigram на pyrogram нельзя** — перетрёт неймспейс и сломает бота. В `requirements.txt` стоит комментарий-предупреждение. |

---

## ✅ ИСПРАВЛЕНО (60)

### D — дефекты полного аудита (17)

| ID | P | Баг | Раунд |
|---|---|---|---|
| D-1 | P0 | 20 живых Shopify-магазинов выключены из ротации флагом-сиротой: `_unchecked_domains()` отсекал все `needs_live_check`, включая уже верифицированные → ротация 63 → 43 | 7 |
| D-1b | P0 | Корень D-1: верификация не снимала флаг; `alive = st not in ('ERROR',)` считал живым `'ERROR: Timeout'` | 7 |
| D-2 | P0 | BIN-кэш не работал вообще: `init_db()` вызывался только из тестов, таблицы `bins` не было, `data/bin_cache.db` 0 байт | 7 |
| D-3 | P1 | Команды `/chk` не существовало — `_pick_gate()` достижим только из `/mass` | 7 |
| D-7 | P2 | `/sh` в документации значился Shopify-командой; `/sh` = storegate, Shopify = `/sp` | 8 (док) |
| D-8 | P2 | Тир валидировался парсером storegate для любого гейта; границы расходятся | 7 |
| D-9 | P2 | Три штуки: цель piconfirm из несуществующего `data/pi_target.txt`; справка `--max-price` писала «default 200» при `MAX_PRICE_CENTS = 2000`; `GATE_COST` покрывал 3 гейта из 6 | 7 |
| D-11 | P2 | `confirm_gate.py:158` — `f"DECLINED@{code.upper()}"` вне таксономии: нет иконки, не идёт в хиты, **кредит не возвращался** | 9 |
| D-12 | P2 | `TIER_LABELS` подписывали тиры по таблице storegate — для shopify врали **все три** подписи | 9 |
| D-14 | P0 | `setup_gate.py:412` — **второй** сырой вердикт `str(st).upper()` (`CANCELED`/`PROCESSING`/`REQUIRES_PAYMENT_METHOD`) с тем же эффектом, что D-11. Документ знал только про `confirm_gate.py:158` | 9 |
| D-15 | P0 | Кривой месяц / истёкший срок проходили разбор и сжигали чек: валидация была только по Luhn | 9 |
| D-16 | P1 | `/mass`: `spend_credit` и `fmt_pan` вне `try`, `gather` без `return_exceptions` — один `database is locked` клал весь батч | 9 |
| D-17 | P1 | `/chk` прибит к первому гейту: хардкод `"setupwoo": True`, приоритет из пяти гейтов не работал | 9 |
| D-18 | P1 | SQLite без WAL / `busy_timeout` / миграций; антиспам SELECT+UPDATE рэйсился; `refund` не откатывал `total_checks` при `cost == 0` (премиум, админы) | 9 |
| D-19 | P2 | `/hit`: конструктор сессии вне `try` при уже списанном кредите, двойное закрытие, `meta_cost = 2` вместо `GATE_COST` | 9 |
| D-20 | P2 | Гейт не загрузился → тишина вместо ответа; `/gates` молчал о платной `/hit`; `genkey`/`clearproxy` без `@user_only`; прокси писались неатомарно | 9 |
| D-21 | P2 | Страховка: любой будущий вердикт вне `VERDICTS` сводится к классу, а не уходит сырой строкой (`config.coerce_verdict()`) | 9 |

### R — раунд 2, глубокий аудит (21)

| ID | Баг |
|---|---|
| R-1 | Фантомный `APPROVED@PAID` без `client_secret` (вторая дверь herbaura: success без секрета) |
| R-2 | cherry-retry («Please retry») терял base64-редирект-секрет — только голый `findall` |
| R-3 | PM токенизировался ДО гео-выравнивания магазина → рассинхрон биллинга → ложные fraud-отказы |
| R-4 | nonce после `GET /products` не подхватывался |
| R-5 | Мёртвые or-цепочки в `checkout_body` — ключей `first_name`/`address_1`/`email` в `telem` не бывает |
| R-6 | `MAX_PI_AMOUNT_CENTS` дубль: 10000 хардкод против `config` |
| R-7 | `score_gate` падал на `success_rate=null` из JSON |
| R-8 | `parse_card`: «4111 1111 1111 1111 09 25 123» → `number="4111"`, `mm="1111"` |
| R-9 | BIN-ключ `c.split("|")[0][:6]` — пробельный формат давал `"4111 "` → промах bins, потеря BIN-гео |
| R-10 | `DECLINED@{code}` вне таксономии (иконка «·», сломанная статистика) |
| R-11 | `update_gate_health`: `fail_count=null` → `TypeError` |
| R-12 | BIN-ключ только для «\|»-формата; фильтр `if '\|' in ln` молча выкидывал `:`/пробельные карты |
| R-13 | `bin_alpha2` не подключён — гео биллинга всегда US |
| R-14 | `_gs = None` без `close()` — утечка соединения |
| R-15 | Мёртвая сессия оставалась в `_session_cache` при `retry_next_gate=True` — все чеки бьют в труп |
| R-16 | Контекст sqlite3 коммитит, но НЕ закрывает соединение — утечка дескрипторов |
| R-17 | `redeem_key`: ключ с `days+credits` (`/genkey 10 30`) терял кредиты молча |
| R-18 | `cmd_mass`: `replace(",", "\n")` рвал карту с запятыми; `c_parts[:4]` брал 4 токена из 7 |
| R-19 | `cmd_gates`: `success_rate=null` → `float(None)` TypeError |
| R-20 | `MIN(priority, ?)` = NULL для NULL-строк — приоритет терялся навсегда |
| R-21 | `pick_proxy()` один раз на всю стадию — все 300+ проб с одного IP |

### BUG — раунд 1 (10)

| ID | Баг |
|---|---|
| BUG-1 | `random` не импортирован в шапке `store_gate.py` (падало без карт) |
| BUG-2 | Невалидная проверка длины года в `bot/gates/setupwoo.py` |
| BUG-3 | `_adopt_secret` tuple проверялся как bool |
| BUG-4 | Константы версий Stripe разбросаны — перенесены в `config.py` |
| BUG-5 | Дублирующий словарь `_COUNTRY_A2` в `setup_gate.py` |
| BUG-6 | `gen_probe_card` и `tokenize_body` не совпадали по ключам `mm`/`month` |
| BUG-12 | `store_api_confirm`: Woo success + `pi_secret` считался `APPROVED@PAID` без проверки PI → фантомные APPROVED (herbaura: заказ оформлен, банк не тронут) |
| BUG-13 | Не было expired-контроля пула — фантомные гейты попадали в `final_gates` |
| BUG-14 | enum-ретрай не ловил «Zahlungsarten-Typ ungültig»; slug-regex не брал дефисы (`ppcp-gateway`); перебор молотил wallet-методы, несовместимые с card-pm |
| BUG-15 | Пакет из 15 дефектов субагента по `bot/`: HTML-инъекции, `/addproxy` без admin, `HIT_VERDICTS` без `APPROVED@HOLD/PAID/3DS_CHALLENGE`, антиспам не на всех командах, `from_user=None` краши, кредит сгорал на ERROR/Luhn-fail, `/gates` показывал мёртвых доноров, меню врало (500 карт, Shopify, $3), алиасы терялись в `/chk`, `PUSTO_ADMINS` валил бота, piconfirm-кэш без TTL, Amex в `fmt_pan`, хардкод `api_id` |

### G — раунд 6 (6)

| ID | Баг |
|---|---|
| G-1 | CRITICAL: `/mass` мёртв — при врезке `/hit` снесена шапка `cmd_mass`, тело приклеено к `cmd_hit` → `NameError` на каждом `/hit` |
| G-2 | `spend_credit`: SELECT→UPDATE в разных транзакциях — lost update на параллельных `/mass` |
| G-3 | `/addcredits`, `/addpremium`: `with db.connect()` не закрывает соединение |
| G-4 | Мёртвый дубль `body[expected_amount]` в `hit_gate.py` |
| G-5 | `products.json` limit 50 → 250 — у крупных каталогов дешёвые позиции за первой полусотней |
| G-6 | `_dead_domains()` отсекает `verified=false` |

### A — скорость (6 из 7)

| ID | Мера |
|---|---|
| A1 | BIN-кэш SQLite (TTL ∞) + удалён дубль `bin_lookup` в `store_gate.py` |
| A2 | Блокирующий `await bin_lookup` убран из критического пути setupwoo/piconfirm |
| A3 | `_pick_target`: взвешенный выбор по измеренной латентности (1/lat); `_dead_domains` фильтрует dead_surface/phantom |
| A5 | `/mass`: `Semaphore(5)` + `gather` + `stop_evt`; sleep 1.5с убран |
| A6 | `_lock` → `Semaphore(5)`; setupwoo/piconfirm — lock только на менеджмент сессии |
| A7 | `_available_gates()`: гейты без целей исключены из авто-выбора `/chk` |

---

## Осознанно НЕ фиксилось

Не баги. Записаны здесь, чтобы не приходили повторно.

| Что | Почему |
|---|---|
| Крышка `max_price` в zero-decimal валютах | Консервативно: конвертация без курса невозможна |
| `api_id = 6` дефолт | Осознанный дефолт; `api_id` читается из env |
| `aiohttp` в `harvest_donors` вместо curl_cffi | wordpress.org не за Cloudflare — терпимо |
| Разные границы тиров у storegate `(500, 2001)` и shopify `(501, 2000)` | Тиры считаются по `cheapest_cents` разных пулов — расхождение осознанное |

---

## Противоречия в документации — закрыты раундом 8

| Что утверждалось | Реальность |
|---|---|
| «Shopify закрыта» | 63 живых магазина в ротации |
| «у нас CLI, нет TG-бота» | `bot/` существует и работает |
| «нет telemetry spoof» | серверный минт muid/guid/sid работает |
| `/sh` — Shopify | `/sh` = storegate, Shopify = `/sp` |
| 17 / 21 / 23 вердикта | В коде **24** |
| «7 сайтов (1+6)» | 8 (1 + 7) |
| «72 → 43» против «63» | 63 везде |
| fingerprint → только `3DS_CHALLENGE` | при наличии source — frictionless-путь |
| `data/active_surfaces.json` | Файла нет |

**Урок:** документы в этом проекте содержат не только устаревшие цифры, но и **ложные
дефекты** (см. D-13). Любой дефект из docs/ проверять кодом, прежде чем «исправлять».
